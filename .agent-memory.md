# OmniFlow · 极简仓库记忆（.agent-memory.md）

> **用途**：后续 Agent 接手本项目时，**先读本文最后 3 条**，30 秒恢复上下文。详细内容去看 `PROJECT_STATUS.md`。
> **写入规则**：只 append，不覆盖。每次 Agent 完成任务在末尾追加 1-3 行总结（100 字以内）。
> **格式**：`## [日期 tag] · [阶段] (commit: XXXXXXX)` → 一行「做了什么」+ 一行「下一步做什么 / 卡在哪」。

---

## 项目一句话（永远第一行）
**汇流 OmniFlow = Web(Vue3+Vite) + PC(Tauri v2) 双端同构的全源聚合规则引擎**，兼容 Legado/TVBox/海阔/ESO/DRPy/CMS/IPTV，Monorepo(pnpm workspace) 架构，核心包 `packages/core` 纯 TS 双端同构。

---

## ⚡ 最快构建顺序（给新 Agent 的 Step-by-Step 恢复）
```
1. cd c:\Users\SATAN\.trae-cn\worktrees\OmniFlow\init-project-plan-R7Bn1J
2. 读文件：PROJECT_STATUS.md §2, §5, §10, §11, §17  +  本文件最后 3 条
3. 终端：git status ; git log --oneline -5
4. 如果有 package.json → pnpm install；没有 → 从 PROJECT_STATUS §11 Step 2 开始建 Monorepo
5. 下一个 MUST 做的 Step = 看 §11 顺序中第一个「验证结果 = ⬜ 待执行」的条目
6. 离开时：更新 PROJECT_STATUS.md L1-L6 + 在本文件末尾追加总结 + git push
```

---

## 🛠 已知工具坑 / 必须前置的配置
| # | 工具 | 坑 | 修复命令（Windows PowerShell） |
|---|---|---|---|
| 1 | Git 长路径 | Windows 260 字符限制 | `git config --global core.longpaths true`（必须先跑） |
| 2 | pnpm 国内源 | 官方源慢 / 超时 | `pnpm config set registry https://registry.npmmirror.com` |
| 3 | Rust 国内源 | crates.io 下载慢 | 新建 `%USERPROFILE%\.cargo\config.toml` 写入中科大镜像（M0 Step14 才需要） |
| 4 | Tauri v2 前置 | 需 WebView2 + MSVC Build Tools | `winget install Microsoft.VisualStudio.2022.BuildTools` → 勾选 C++ 桌面开发 |
| 5 | worktree + node_modules | 不同 worktree 共享 node_modules 冲突 | 每个 worktree 独立安装，**不要**用 `pnpm install --shared-workspace-lockfile=false` 以外的 hack |

---

## 📁 关键路径速查
| 你想找什么 | 路径（绝对） |
|---|---|
| 项目根目录 | `c:\Users\SATAN\.trae-cn\worktrees\OmniFlow\init-project-plan-R7Bn1J\` |
| 项目状态文档（一切的入口） | `PROJECT_STATUS.md` |
| 开发规划 v3.0（技术细节） | `开发规划.md` （未来移到 `docs/开发规划.md`） |
| UI 原型（当前版） | `index.html` / `style.css` / `script.js`（M0 Step12 后移到 prototype/） |
| 待办优先级清单 | `PROJECT_STATUS.md §5` |
| 下一步执行顺序（14 步 M0） | `PROJECT_STATUS.md §11` |
| 验证命令 + 填空表格 | `PROJECT_STATUS.md §14` |
| 交接协议（接手/离开规则）| `PROJECT_STATUS.md §15` |
| M0 验收 10 项 | `PROJECT_STATUS.md §16.1` |
| 可复制提示词模板（新对话用） | `PROJECT_STATUS.md §19` |
| Monorepo 包图 | `PROJECT_STATUS.md §7 变更文件地图` |
| 技术选型 ADR（12 项决策） | `开发规划.md §4.1` |
| 规则引擎 v2 段-步流水线 | `开发规划.md §8.1`（**M1 开发必读，不要按 v1 简化实现！**） |
| IR 类型定义蓝图 | `开发规划.md §7` |

---

## 🧭 里程碑编号速记
```
M0 = 基建 Monorepo + Web 空壳 + Vue 原型迁移 (2周)
M1 = 核心规则引擎 ★ 关键路径 (4周)
M2 = QuickJS 沙箱 + hiker/drpy 兼容 (3周)
M3 = 书籍阅读闭环 (3周)  ← 可与 M4 并行
M4 = 影视播放闭环 (3周)  ← 可与 M3 并行
M5 = 直播 + IPTV + EPG (2周)
M6 = 规则工坊 Monaco 编辑器 + 调试器 + v0.1.0 发布 (2周)
关键路径：M0 → M1 → M2 → (M3 ‖ M4) → M5 → M6
```

---

## 📋 历史记录（从下往上读，最新在最底部）
> **每次 Agent 结束，在下方追加。格式：日期 · 阶段 · commit · 一句话总结 + 下一步/卡点。**

---

## [2026-08-29 #001] · 项目初始化基线 (commit: b1a4e6c)
**做了什么**：建立 Git 仓库 + worktree 分支；完成开发规划 v3.0（19 章）；交付 15 屏可交互 UI 原型（HTML/CSS/JS）；生成 PROJECT_STATUS.md v1.0（19 节全量状态文档、交接协议、提示词模板）；生成本 .agent-memory.md 极简记忆文件。
**当前状态**：M0 30%。Monorepo 骨架未创建（Step 2 起）。
**下一步 MUST**：用户确认 GitHub 仓库 URL（阻塞 B1）和原型归档策略（B3）→ 执行 §11 M0 Step 2（package.json + pnpm-workspace.yaml）。
**卡点**：🚨 B1=GitHub URL 未给；B3=归档策略待确认；Rust 工具链未验证（桌面端 P2）。

---

## [2026-08-29 #002] · 用户确认 + GitHub 首次同步 (commit: 5d9a6b1)
**做了什么**：用户确认 3 项：① GitHub URL=https://github.com/yoursatan/OmniFlow.git ② 原型归档=方案A(git mv 保留历史) ③ Rust 未安装→M0 桌面端 P2 跳过；远程 origin 已配置并首次 push 分支成功；PROJECT_STATUS.md 阻塞项表/执行顺序/基线表/提示词模板A/版本日志 5 处更新。
**当前状态**：M0 30%。0 个活跃阻塞。准备启动 M0 Step 2。
**下一步 MUST**：PROJECT_STATUS.md §11 M0 Step 2 → 根级 package.json + pnpm-workspace.yaml + turbo.json（执行后跑 pnpm install 验证）。
**卡点**：无。

## [2026-08-29 #003] · M0 Step 2-5 Monorepo 骨架落地 (commit: 78d6d46 / 骨架 b693f11)
**做了什么**：创建根 Monorepo 4 文件(package.json/pnpm-workspace.yaml/turbo.json/.npmrc) + 工具链 4 文件(tsconfig.base/.eslintrc/.prettierrc/.editorconfig) + packages/shared 4 TS 类型文件（source/content/engine 60+ 类型对齐§7 蓝图）+ packages/core 空壳(8子目录+package.json+index.ts re-export shared)；4 步 typecheck 全部 exit=0，tsup build 全部成功；工具链 4 个新坑(T7-T10)写进 PROJECT_STATUS §13；§5/§14/§17/§18 同步更新。
**当前状态**：M0 完成度 60%。Step 2-5 全部通过。下一个 Step = 6。
**下一步 MUST**：PROJECT_STATUS.md §11 M0 Step 6 → 创建 apps/web Vue3+Vite+TS 空壳（package.json / vite.config.ts / index.html / src/main.ts / App.vue / router/ ×15 路由 / store/ Pinia），最后 `pnpm dev -F web` 看到页面才算通过。
**卡点**：⚠️ 小问题：pnpm v11 `onlyBuiltDependencies` 命名机制变化（见 §13 T7/T10），导致 `pnpm --filter <pkg>` 做任何命令都会先触发 install 校验失败；**解决办法 = 关键命令直接用本地二进制 `node_modules\.bin\tsc.cmd` / `npx --no-install <cmd>` / `.\node_modules\.bin\vite.Cmd`，不要走 pnpm --filter 包装**。另一个 Win PowerShell 陷阱：T8 写 TS 文件必须是 UTF-8 NO-BOM。这两个是必记坑！

---

<!-- 追加模板（复制粘贴填完）
## [YYYY-MM-DD #NNN] · [阶段名] (commit: XXXXXXX)
**做了什么**：<一句话，例：M0 Step 2-5：创建 workspace/shared/core/web 空壳 + TS/ESLint 配置。>
**当前状态**：<M0 完成度 XX%，Step 5 已通过，Step 6 进行中。>
**下一步 MUST**：<从 §11 找下一个 Step，例：M0 Step 6 → apps/web 接入 Vue Router 15 路由。>
**卡点**：<如果有，否则写 无>
-->
